export type View = "table" | "statistics" | "map" | "analysis"

// The order the "V" shortcut cycles through, and the order the toolbar shows.
export const VIEWS: View[] = ["table", "statistics", "map", "analysis"]
